#pragma once

void incInsertionSort(int arr[], size_t size);
void decInsertionSort(int arr[], size_t size);
int linearSearch(int v, int arr[], size_t size);
bool* addBin(bool a[], bool b[], size_t size);